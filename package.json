{
  "name": "forex_calculator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix forexflow-frontend\" \"npm run dev --prefix forexflow-backend\"",
    "install-all": "npm install && npm install --prefix forexflow-frontend && npm install --prefix forexflow-backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
